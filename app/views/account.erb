<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
    <p>Username:<%=Helpers.current_user(session).username %></p>
    <p>Your Balance is:<%=Helpers.current_user(session).balance%></p>

    <h1><a href="/logout">Log Out</a></h1>
    <%end%>
  </body>
</html>
